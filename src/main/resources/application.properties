# Server Configuration
server.port=${PORT:8080}

# Application Info
spring.application.name=bfhl-api

# Jackson Configuration - use snake_case for JSON
spring.jackson.property-naming-strategy=SNAKE_CASE
